/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "tailwindui.com",
      "via.placeholder.com",
      "fakestoreapi.com",
    ],
  },
};

module.exports = nextConfig;
